{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window desktop permissions grouped by current local app features. Split this capability before adding sync, filesystem, shell, or broader network powers.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "process:default",
    "store:default",
    "updater:default"
  ]
}
